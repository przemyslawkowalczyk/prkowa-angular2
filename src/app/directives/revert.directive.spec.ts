import { RevertDirective } from './revert.directive';

describe('RevertDirective', () => {
  it('should create an instance', () => {
    const directive = new RevertDirective();
    expect(directive).toBeTruthy();
  });
});
